import { create } from "zustand";

// interface useBannerStore {
//     loading?: boolean;
//     image?: string;
//     description?: string;
// }

// const useBanner = create<useBannerStore>((set) => ({
//     loading: false,
//     image,
//     description: () => set({isOpen: false})
// }));

// export default useBanner;